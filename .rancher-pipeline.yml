stages:
- name: Test
  steps:
  - runScriptConfig:
      image: node:latest
      shellScript: |-
        npm install
        npm run build
        npm run start
        npm run tests
timeout: 60
notification: {}
