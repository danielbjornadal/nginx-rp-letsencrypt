stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: bjornadalno/nginx-rp-letsencrypt:test
      pushRemote: true
      registry: index.docker.io
    env:
      PLUGIN_DEBUG: "true"
timeout: 60
notification: {}
